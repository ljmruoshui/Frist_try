template <typename T>
void swap(const T &a, const T &b)
{
    T temp = a;
    a = b;
    b = temp;
    
};


template <typename T> inline T judge(T &right, T &wrong);

template <class cls> class queue{
    public:
    private:
};

//template <> 
//int compare<const char *> (const char * const &p, const char * const &q);
